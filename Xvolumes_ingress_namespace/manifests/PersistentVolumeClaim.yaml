apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-claim
  namespace: todo-namespace
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi