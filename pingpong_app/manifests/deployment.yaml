apiVersion: apps/v1
kind: Deployment
metadata:
  name: pingpong-app-dep
  namespace: todo-namespace
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pingpong-app
  template:
    metadata:
      namespace: todo-namespace
      labels:
        app: pingpong-app
    spec:
#      volumes:
#        - name: shared-visits
#          persistentVolumeClaim:
#            claimName: data-claim
      containers:
        - name: pingpong-app
          image: koskinentht/pingpong-app:v1.0
          imagePullPolicy: "Always"
#          volumeMounts:
#          - name: shared-visits
#            mountPath: /usr/src/app/files